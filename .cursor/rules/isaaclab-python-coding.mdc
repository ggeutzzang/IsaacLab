# IsaacLab Python 코드 규칙

## 환경 설정
```python
from isaaclab.app import AppLauncher
from isaaclab_tasks.utils import parse_env_cfg
import gymnasium as gym

# Isaac Sim 런타임 초기화
app_launcher = AppLauncher(headless=False, enable_cameras=True)
simulation_app = app_launcher.app
```

## 환경 생성 패턴
```python
# 환경 설정 파싱
env_cfg = parse_env_cfg(env_id)

# 환경 수 설정 (성능 최적화)
env_cfg.scene.num_envs = 1

# 환경 생성
env = gym.make(env_id, cfg=env_cfg)
```

## 조인트 이름 규칙
- **URDF 원본**: `shoulder_pan`, `shoulder_lift`, `elbow_flex`, `wrist_flex`, `wrist_roll`, `gripper`
- **Isaac Lab 설정**: 동일한 이름 사용 (접미사 `_joint` 제거)

## USD 파일 경로
```python
# 로컬 USD 파일 사용
usd_path = "/home/iamhjoo/myprojects/IsaacLab/tmp/so100_assets/so100.usd"
```

## 에러 처리
```python
try:
    env = gym.make(env_id, cfg=env_cfg)
    print("✅ 환경 생성 성공")
except Exception as e:
    print(f"❌ 환경 테스트 중 오류 발생: {e}")
    import traceback
    traceback.print_exc()
```

## 환경 정보 출력
```python
print(f"관찰 공간: {env.observation_space}")
print(f"행동 공간: {env.action_space}")
print(f"초기 관찰 키: {list(obs.keys())}")
```

## 테스트 파일 위치
- **권장**: `scripts/` 디렉토리
- **비권장**: `scripts/tests/` (gitignore에 의해 무시됨)

## 성능 최적화
- `num_envs = 1`: 메모리 사용량 최소화
- `headless=True`: GUI 없이 실행
- `enable_cameras=False`: 카메라가 필요하지 않은 경우
description:
globs:
alwaysApply: false
---
 